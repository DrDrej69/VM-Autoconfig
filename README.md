# VM-Autoconfig

# einmalig git installieren
sudo apt update && sudo apt install -y git

# dein Repo klonen
git clone https://github.com/DrDrej69/VM-Autoconfig

# Skript ausführbar machen und ausführen
chmod +x setup-ssh.sh
sudo ./setup-ssh.sh adminuser
